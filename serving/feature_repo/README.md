# Feast Feature Repo

The canonical Feast repository now lives at the project root in
`feature_repo/`.

This nested `serving/feature_repo/` copy is kept only for historical context and
should not be treated as the source of truth for the current online inference
flow.
